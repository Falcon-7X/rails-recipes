<% if @event.logo.present? %>
  <%= link_to image_tag(@event.logo.url(:thumb_large)), @event.logo.url, :target => "_blank" %>
<% end %>

<h1><%= @event.name %></h1>

<%= sanitize @event.description %>
<h2><%= @event.category.try(:name) %></h2>
<h2><%= display_event_status(@event) %></h2>
<!-- 方法2 -->
<!-- <h3><%= t(@event.status, :scope => "event.status") %></h3> -->

<p><%= link_to "开始报名", new_event_registration_path(@event), :class => "btn btn-primary" %></p>
